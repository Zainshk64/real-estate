function TagPill({ children }) {
  return (
    <span className="rounded-pill bg-sand/20 px-3 py-1 text-xs font-medium uppercase tracking-[0.2em] text-forest">
      {children}
    </span>
  )
}

export default TagPill
